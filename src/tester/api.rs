//! The tester's public interface: hand it a request, get back a response or
//! `None` if the path is not the tester's, plus the switch that decides whether
//! it answers at all.

pub use super::Tester;
